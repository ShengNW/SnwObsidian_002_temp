# 2026-04-16 本地网络环境严格无侵入调研报告

## 1. 调研范围与约束
- 调研时间：2026-04-16T11:11:23+00:00（UTC）
- 主机：`vps-47db6a55`
- 系统：`Linux 6.14.0-37-generic (Ubuntu)`
- 约束：全程只执行只读命令（状态查询），未执行端口扫描、未改配置、未重启服务、未发起压测。

## 2. 采集方法（只读）
使用的命令仅包含：
- `ip -brief addr`、`ip route`、`resolvectl status`
- `ss -tulpen`、`ss -tn state established`
- `lsof -nP -iTCP -sTCP:LISTEN`、`lsof -nP -iUDP`
- `systemctl list-units --type=service --state=running`
- `docker ps`
- `ufw status verbose`、`nft list ruleset`
- `curl https://api.ipify.org`、`curl https://ifconfig.me/ip`、`curl https://api64.ipify.org`、`curl https://ipinfo.io/json`

## 3. 公网与基础网络
- 公网 IPv4：`51.75.133.235`
- 公网 IPv6：`2001:41d0:52:bff::60d`
- 反向主机名：`vps-47db6a55.vps.ovh.net`
- ASN：`AS16276 OVH SAS`
- 默认网关：`51.75.133.1`（`ens3`）
- DNS（上游）：`213.186.33.99`
- DNS stub：`127.0.0.53`

本机接口（摘要）：
- `ens3`：公网口（IPv4/IPv6）
- `docker0`：`172.17.0.1/16`
- `br-5bbedacd0705`：`172.18.0.1/16`
- `br-2adb8085cfeb`：`172.19.0.1/16`
- 多个 `veth*`：容器虚拟网卡

## 4. 监听端口与服务映射（核心）

### 4.1 对外监听（非 loopback）
| 协议 | 端口 | 进程/服务 | 说明 |
|---|---:|---|---|
| TCP | 22 | `sshd` | SSH |
| TCP | 80 | `nginx` | HTTP |
| TCP | 443 | `sslh` | SSL/SSH 多路复用入口（转发 SSH/TLS） |
| TCP | 8443 | `nginx` | TLS 后端端口（被 `sslh` 使用） |
| TCP | 5432 | `docker-proxy` -> `postgres` 容器 | PostgreSQL（对外绑定） |
| TCP | 9876 | `v2ray` | V2Ray 入口 |
| TCP | 3011 | `router` | 自定义路由服务 |
| TCP | 21115 | `hbbs`（RustDesk） | RustDesk NAT |
| TCP | 21116 | `hbbs`（RustDesk） | RustDesk ID |
| UDP | 21116 | `hbbs`（RustDesk） | RustDesk ID |
| TCP | 21117 | `hbbr`（RustDesk） | RustDesk Relay |
| TCP | 21118 | `hbbs`（RustDesk） | RustDesk |
| TCP | 21119 | `hbbr`（RustDesk） | RustDesk |

### 4.2 本地监听（仅 loopback）
| 协议 | 地址:端口 | 进程/来源 |
|---|---|---|
| TCP/UDP | `127.0.0.53:53` / `127.0.0.54:53` | `systemd-resolved` |
| TCP | `127.0.0.1:5244` | `docker-proxy` -> `alist` |
| TCP | `127.0.0.1:8000` | `docker-proxy` -> `list-api` |
| TCP | `127.0.0.1:8501` | `docker-proxy` -> `list-ui` |
| TCP | `127.0.0.1:2222` / `127.0.0.1:2223` | `sshd-session`（隧道/转发会话） |

## 5. 防火墙（UFW/nft）
- UFW 状态：`active`
- 默认策略：`deny (incoming), allow (outgoing), deny (routed)`
- 已放行（关键）：`22/tcp`、`80/tcp`、`443/tcp`、`8443`、`9876/tcp`、`21115/tcp`、`21116/tcp+udp`、`21117/tcp`、`3022/tcp`、`5432/tcp(from 51.15.210.14)`（含 v6 规则）

说明：`5432` 在进程层面对 `0.0.0.0/[::]` 监听，但 UFW 仅允许来源 `51.15.210.14` 访问该端口。

## 6. 正在运行的网络相关服务
systemd 运行中的关键项（节选）：
- `docker.service`、`containerd.service`
- `nginx.service`
- `ssh.service`
- `sslh.service`
- `v2ray.service`
- `systemd-networkd.service`
- `systemd-resolved.service`

容器（`docker ps` 摘要）：
- `hbbs` / `hbbr`（`rustdesk/rustdesk-server`）
- `postgres`（`0.0.0.0:5432->[container]:5432`）
- `alist`（`127.0.0.1:5244`）
- `list-api`（`127.0.0.1:8000`）
- `list-ui`（`127.0.0.1:8501`）
- `list-worker`、`list-redis`（容器内部端口）

## 7. 已建立连接快照（非侵入观察）
说明：以下为采样瞬间状态，连接随时间变化。

远端主机（按连接数粗略排序，剔除本机回环/本机公网地址）：
- `219.147.5.158`（22）
- `88.166.3.42`（7）
- `221.130.30.75`（5）
- 其余为少量单连接（Cloudflare/GitHub/Google/AWS 等地址）

远端端口分布（剔除本机回环/本机公网地址）：
- `443`（15）
- `5432`（3）
- 其余为少量高位端口（多与 `9876` 会话相关）

## 8. 结论（当前快照）
- 网络形态为“公网入口 + Docker 桥接 + UFW 控制”的典型 VPS 架构。
- 对外暴露面主要集中在：`22/80/443/8443/9876/21115-21119/3011`，以及来源受限的 `5432`。
- `sslh` 正在承担 `443` 入口复用，后端指向 `127.0.0.1:22` 与 `127.0.0.1:8443`。
- 存在规则-监听不完全对齐：`3022/tcp` 已放行但当前未见监听进程。
- 本报告全过程为无侵入只读调查，未对现网服务造成配置变更或主动探测负载。
